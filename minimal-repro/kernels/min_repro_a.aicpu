/* Minimal AICPU kernel A. Identical to kernel B except the entry symbol
 * name (same length), so both compile to EXACTLY equal payload bytes. */
#include "aicpu_api.h"

__global__ __aicpu__ uint32_t MinReproKernelA(void *args)
{
    (void)args;
    return 0;
}
